import { Sprite, Texture } from 'pixi.js';

class Background extends Sprite {
    constructor(texture: Texture) {
        super(texture);
        // Set up the background sprite properties
    }

    // Additional methods for background if needed
}

export default Background;
