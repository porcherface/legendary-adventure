import { Sprite, Texture } from 'pixi.js';

class Card extends Sprite {
    constructor(texture: Texture) {
        super(texture);
        // Additional setup...
    }
    // Additional methods...
}

export default Card;
